syntax = "proto3";

package pb;

message Message {
    string name = 1;     // Sender's name
    string receiver = 2; // Receiver's name
    string text = 3;     // Text data
    bytes file_data = 4; // File data
}
